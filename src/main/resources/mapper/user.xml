<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.Read_Write.Lock.Pattern.mapper.UserMapper">
    <select id="getUserById" resultType="com.example.Read_Write.Lock.Pattern.dto.User">
        SELECT * FROM user WHERE id = #{id}
    </select>
</mapper>